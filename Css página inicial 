body {
    background-color: red;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    height: 100vh;
    margin: 0;
    text-align: center;
}

/* Container das Imagens */
.container-imagens {
    display: flex;
    width: 80%;
    max-width: 900px; /* Evita que as imagens fiquem muito espalhadas */
    justify-content: space-between; /* Espaçamento balanceado */
    align-items: center;
    gap: 30px;
}

/* Estilização das imagens */
.container-imagens img {
    width: 250px;
    height: auto;
}

/* Botões */
.botoes {
    margin-top: 20px;
}

button {
    background-color: transparent;
    border: 3px solid orange;
    color: white;
    font-size: 18px;
    padding: 10px 20px;
    margin: 10px;
    border-radius: 50px;
    cursor: pointer;
    transition: 0.3s;
}

button:hover {
    background-color: darkorange;
    color: black;
}

/* Responsividade */
@media (max-width: 800px) {
    .container-imagens {
        flex-direction: column; /* Empilha as imagens no celular */
        gap: 15px;
    }

    .container-imagens img {
        width: 200px;
    }
}