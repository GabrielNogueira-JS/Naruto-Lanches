@import url('https://fonts.googleapis.com/css2?family=Bree+Serif&display=swap');

body {
    background: linear-gradient(to right, rgb(255, 182, 193), rgb(128, 0, 128));
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    flex-direction: column;
    font-family: "Bree Serif", Arial, sans-serif;
    color: white;
    margin: 0;
    padding: 0;
}

.container {
    max-width: 1300px;
    width: 90%;
    background-color: rgba(0, 0, 0, 0.6);
    padding: 20px;
    border-radius: 20px;
    box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.5);
}

/* Título */
.container h2 {
    text-align: center;
    margin-bottom: 20px;
}

/* Layout flexível */
.flex-container {
    display: flex;
    align-items: center;
    justify-content: space-between;
}

/* Ajuste das imagens laterais */
.side-image {
    width: 15%; /* Define a largura da imagem */
    display: flex;
    justify-content: center;
    align-items: center;
}

.side-image img {
    width: 100%; /* Garante que a imagem ocupe toda a largura do container */
    max-width: 150px; /* Limita o tamanho da imagem */
    border-radius: 10px; /* Adiciona bordas arredondadas */
}

/* Container dos alimentos */
.desserts-container {
    display: flex;
    justify-content: space-between;
    width: 65%;
}

/* Colunas da lista */
.left-column, .right-column {
    width: 50%;
}

/* Estilização dos alimentos */
.ingredient {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 15px;
    padding: 10px;
    border: 2px solid #333;
    border-radius: 10px;
    background-color: #fff;
    color: black;
    font-family: "Bree Serif", serif;
}

/* Nome da sobremesa */
.ingredient strong {
    font-size: 18px;
    color: black;
    margin-right: 10px;
}

/* Ingredientes em itálico */
.ingredients {
    font-size: 14px;
    font-style: italic;
    margin: 5px 0;
    color: black;
}

/* Botões */
button.increment, button.decrement {
    background-color: black;
    border: 2px solid black;
    color: white;
    font-size: 14px;
    padding: 6px 12px;
    margin: 0 5px;
    border-radius: 8px;
    cursor: pointer;
    transition: 0.3s;
    font-family: "Bree Serif", serif;
}

button.increment:hover, button.decrement:hover {
    background-color: white;
    color: black;
}

/* Total de sobremesas */
#total, #valor-total {
    font-size: 18px;
    font-weight: bold;
    margin-top: 15px;
    color: orange;
    text-align: center;
}

/* Botão de retorno */
#return-button {
    background-color: black;
    border: 2px solid black;
    color: white;
    font-size: 16px;
    padding: 10px 20px;
    margin: 20px auto 0 auto;
    border-radius: 10px;
    cursor: pointer;
    transition: 0.3s;
    display: block;
    font-family: "Bree Serif", serif;
}

#return-button:hover {
    background-color: white;
    color: black;
}
